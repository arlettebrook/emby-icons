{
  "name": "emby-icons-admin",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --kv EMBY_ICONS --persist-to .wrangler/state",
    "deploy": "wrangler pages deploy public --project-name emby-icons",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.26.1"
  }
}
