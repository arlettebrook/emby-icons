{
  "name": "emby-icons-admin",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "wrangler pages dev public --kv EMBY_ICONS --d1 DB=emby-icons-local --persist-to .wrangler/state",
    "db:migrate:local": "wrangler d1 migrations apply emby-icons-local --local --persist-to .wrangler/state",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.26.1"
  }
}
